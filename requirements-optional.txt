# Optional OCR/document engines. Install what you want to benchmark.
paddleocr>=2.7
paddlepaddle-gpu>=2.6; platform_system == "Linux"
easyocr>=1.7
surya-ocr>=0.6
docling>=2.0

# Required for Qwen2.5-VL vision processor (image token handling)
qwen-vl-utils>=0.0.8
