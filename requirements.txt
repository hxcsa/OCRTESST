pyyaml>=6.0.1
numpy>=1.24
pandas>=2.0
pillow>=10.0
opencv-python>=4.8,<4.12
pdf2image>=1.17
tqdm>=4.66
tabulate>=0.9
torch>=2.1
transformers>=4.49
accelerate>=0.33

# Optional VLM/TrOCR support uses torch/transformers above.
# Install OCR/document engines from requirements-optional.txt as needed.
# System package poppler-utils is required by pdf2image for PDFs.
